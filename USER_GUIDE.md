# Koto 用户指南

本指南面向下载 Windows 版本后第一次使用 Koto 的用户。开发、测试和打包
说明请使用 [文档入口](DOCUMENTATION_INDEX.md)。

## 1. 下载安装

从 [Koto Releases](https://github.com/Loganwon/Koto/releases/latest) 下载最新
稳定版。通常选择 `Koto_v*_Setup.exe`；它会创建开始菜单入口。若需要放在
U 盘或不希望安装，下载 `Koto_v*_Windows.zip`，解压后运行
`Start_Koto.bat`。

首次启动后，在浏览器打开 Koto 显示的本地地址（通常为
`http://127.0.0.1:5000/`）。Koto 默认在本机运行；关闭窗口不会把你的文件
自动上传到 Koto 服务器。

## 2. 配置模型

在设置中添加可用模型的 API Key，或连接你已运行的本地模型。使用 DeepSeek
时，填写 API Key 后保存即可。不要把 API Key 发送到聊天、Issue、截图或文件
任务中。

如果暂时没有模型，仍可浏览工作区和设置；发送任务前 Koto 会提示你完成配置。

## 3. 完成第一个任务

1. 新建一个对话或打开工作区。
2. 拖入文件，或在文件区选择已有文件。
3. 用一句明确的话描述目标、输出和限制。
4. 在任务进度完成后查看结果；写入任务会显示产物或修改报告。

可以直接尝试：

```text
总结这份 PDF，列出三个关键结论和两个待确认问题。
```

```text
把销售表按地区汇总，生成图表，并把结论写入一份新的 DOCX 报告。
```

```text
检查这份合同的风险条款。先给建议，不要修改原文件。
```

## 4. 文件任务的安全使用

- 先用“只分析，不要修改”确认方向；满意后再要求生成或写入文件。
- 在任务中说明目标文件、预期格式和是否允许覆盖原文件。
- 任务结束后查看产物路径与修改报告，再自行确认内容是否符合业务要求。
- 不要把密码、API Key、身份证号或不应交给模型处理的敏感数据放入提示词。

Koto 会在任务中呈现状态和结果，但 AI 输出仍需要你的专业复核，尤其是合同、
财务、医疗、法律和生产配置等高风险内容。

## 5. 日常使用建议

- 将一个复杂目标拆成“分析 → 确认 → 写入”三步，结果更稳定。
- 继续追问时说明“基于刚才的结果”，并补充新的限制或素材。
- 任务失败时先保留任务描述、时间和错误提示，然后按
  [支持与反馈](SUPPORT.md) 的步骤排查。

## 6. 更新 Koto

从 Releases 下载新版本后运行新的安装程序即可更新。使用便携版时，先备份你
自己的工作区与配置，再解压新版本到新的文件夹进行验证。
