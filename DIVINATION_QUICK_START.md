# Koto 占卜功能改进 - 快速使用指南

## 🎯 核心改进

您提出的反馈已全部解决：

> **旧问题**："占卜以后连预测结果都给不出来，太空了"
>
> **新方案**：占卜现在结合数据给出**具体、有根据的倾向性预测**

---

## ✨ 新功能演示

### 场景 1️⃣：体育比赛预测（数据驱动）

**用户提问**：
```
BLG vs G2，谁会赢？
```

**改进前**：
```
✦ 星辰低语，命运之轮转动...
权杖五显示激烈竞争，两方势均力敌...
所有可能都存在于此刻，选择权在你手中...
```
❌ 太虚幻，没有具体判断

**改进后**：
```
📊 【数据分析】
• BLG：LPL 排名 2，最近胜率 70%，队伍评分 78/100
• G2：LEC 排名 3，最近胜率 58%，队伍评分 73/100  
• 历史对阵：BLG 赢过 2 次

🎴 【牌面洞察】
权杖六·正位（胜利之牌）
→ 指向实力更强、准备更充分的一方 = BLG

✨ 【综合预测】
基于数据与牌面，BLG 的胜率约为 **65-70%**
置信度：75%

💡 【建议】
虽然 BLG 更占优势，但 G2 仍有 30% 的翻盘机会。
如果下注，倾向 BLG；如果博彩，做好准备迎接意外。
```
✅ 具体、有依据、给出明确判断

---

### 场景 2️⃣：感情关系咨询（纯占卜）

**用户提问**：
```
我该不该和他复合？
```

**效果**（无量化数据，保持纯占卜解读）：
```
🎴 【牌面洞察】
抽到「倒吊人·正位」——暂停与反思之牌
这不是"留"或"走"，而是说你需要换个角度看这段关系

✨ 【我的判断】
现在不是复合的时机，而是理清思路的时机

💡 【建议】
这个月列出复合的 3 个原因和保持距离的 3 个理由
然后和他进行一次坦诚对话，可能会有新发现
```
✅ 保留占卜的深度，但给出明确指引

---

## 📊 技术实现

### 新增的文件

| 文件 | 功能 |
|------|------|
| `app/core/skills/divination_data_handler.py` | 数据处理引擎（~450行） |
| `config/divination_data/esports_teams.json` | 体育队伍数据库 |
| `config/skills/divination.json` | 更新的占卜提示词 |
| `test_divination_enhancement.py` | 功能测试脚本 |
| `DIVINATION_ENHANCEMENT_REPORT.md` | 详细技术报告 |

### 改进的文件

| 文件 | 改动 |
|------|------|
| `app/core/skills/skill_manager.py` | 增强 `_normalize_divination_prompt()`、新增 `_get_divination_data_guidance()` |

---

## 🚀 立即体验

### 方式 1：运行测试脚本

```bash
cd c:\Users\12524\Desktop\Koto
python test_divination_enhancement.py
```

**预期输出**：
- ✓ 检测到问题领域（体育、感情等）
- ✓ 提取关键实体（队伍名、日期等）
- ✓ 生成数据驱动预测
- ✓ 计算百分比倾向性

### 方式 2：在 Koto 中启用占卜技能

1. 打开 Koto
2. 启用"塔罗占卜"技能
3. 提问：`BLG vs G2 谁会赢？`
4. 等待 AI 返回 **具体的数据驱动预测**

---

## 💡 工作原理

```
用户提问
    ↓
分析问题领域 ← "比赛"、"队伍"、"谁赢" → 识别为「体育比赛」
    ↓
提取实体 ← "BLG、G2" → 获取队伍名
    ↓
查询本地数据 ← `config/divination_data/esports_teams.json`
    ↓
生成数据指导 ← 添加到占卜提示词中
    ↓
AI 生成增强的占卜解读 ← 结合数据 + 牌义 + 倾向性判断
```

---

## 📈 何时使用数据驱动 vs 纯占卜

### ✅ 自动获得数据驱动分析的问题

```
体育类：
  - "明天的 LOL 比赛 T1 能赢吗？"
  - "这个赛季谁能成为 MVP？"
  - "BLG vs FunPlus，胜率多少？"

财经类：
  - "腾讯股票明天会涨吗？"
  - "这基金值得投资吗？"

天气类：
  - "明天北京会下雨吗？"
  - "本周末适合露营吗？"
```

### ❌ 保持纯占卜（无数据）的问题

```
感情类：
  - "我该和他分手吗？"
  - "这段关系有未来吗？"

职业类：
  - "我该离职吗？"
  - "这个机会好不好？"

心灵类：
  - "我最近运气怎样？"
  - "我应该如何突破困境？"
```

---

## 🔧 扩展数据源

### 添加新的队伍数据

编辑 `config/divination_data/esports_teams.json`：

```json
{
  "teams": {
    "EDG": {
      "name": "EDG",
      "full_name": "Edward Gaming",
      "region": "LPL",
      "win_rate": 0.62,
      "recent_form_score": 70,
      "rank": 4,
      "recent_matches": [...]
    }
  }
}
```

### 添加新的数据领域

编辑 `app/core/skills/divination_data_handler.py`：

```python
DOMAIN_PATTERNS = {
    'your_domain': {
        'keywords': ['关键词1', '关键词2', ...],
        'event_types': ['prediction_type'],
    }
}

def _predict_your_domain(self, context, tarot_cards):
    # 实现您的预测逻辑
    pass
```

---

## 🎴 占卜模式对比

| 方面 | 改进前 | 改进后 |
|------|--------|--------|
| **具体性** | 模糊、多解 | 明确、单向 |
| **数据支撑** | 无 | 有（可用时） |
| **预测格式** | "可能性存在" | "胜率 65-70%" |
| **置信度** | 不明说 | 明确标示 |
| **执行空间** | 模棱两可 | 具体建议 |

---

## ✅ 功能检查清单

验证改进是否生效：

- [ ] 体育比赛预测时给出 **百分比胜率**
- [ ] 感情问题 **不** 生成数据（保持纯占卜）
- [ ] 提示词中包含 **数据驱动分析** 相关内容
- [ ] 历史对阵信息被 **正确引用**
- [ ] 倾向性判断 **明确而非模糊**

---

## 📞 问题排查

**Q: 为什么还是没给出具体预测？**
A: 可能是：
1. 占卜技能未启用 → 检查技能列表
2. 问题没有本地数据 → 尝试其他体育队伍
3. 问题类型不在支持列表 → 参考"何时使用数据驱动"

**Q: 我想添加更多体育队伍？**
A: 编辑 `config/divination_data/esports_teams.json`，按格式添加新队伍

**Q: 可以集成实时 API 吗？**
A: 可以！参考 `DIVINATION_ENHANCEMENT_REPORT.md` 中的"后续可扩展方向"部分

---

## 🌟 预期体验提升

| 场景 | 改进前 | 改进后 |
|------|--------|--------|
| 比赛预测 | "牌面显示可能..." | "BLG 65% 胜率，因为..." |
| 决策辅助 | "选择权在你手" | "倾向 A，但 B 有 30% 可能" |
| 用户信任 | 🤔 半信半疑 | ✅ 有数据有依据 |

---

## 🎯 下一步

1. **今日**：启用占卜技能，尝试提问一个比赛预测
2. **本周**：根据反馈调整数据权重或添加新队伍
3. **本月**：考虑集成外部 API（体育数据、天气等）

---

**感谢您的建议！** 这个改进使 Koto 的占卜功能从"虚无缥缈"升级到"具体可信"。⭐
