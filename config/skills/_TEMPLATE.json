{
  "_comment": [
    "====================================================================",
    "  Koto Skill 创建模板  ─  参考 Anthropic 提示工程最佳实践",
    "====================================================================",
    "",
    "【Anthropic 做了什么？】",
    "  Anthropic 的 Prompt Library 和 Claude.ai Projects 遵循同一套结构：",
    "    1. 明确角色（Role）         ← 你在 prompt 里的 '你是一个...'",
    "    2. 明确任务（Task）         ← 遇到什么输入，要做什么",
    "    3. 少样本示例（Examples）   ← 最关键：2-3 个输入/输出例子",
    "    4. 输出格式（Format）       ← 精确告知格式，否则模型随机发挥",
    "    5. 约束规则（Constraints） ← 不允许做什么",
    "",
    "  核心结论：小白想让 skill 好用，最重要的不是写复杂的 prompt，",
    "  而是 '写够 2-3 个具体例子'。模型会从例子中学到细节，",
    "  就算 prompt 写得很简单，效果也会远优于没有例子的长篇 prompt。",
    "",
    "【如何使用本模板】",
    "  1. 把这个文件复制成 my_skill.json（文件名 = skill id，小写+下划线）",
    "  2. 删掉所有 _comment 字段",
    "  3. 填写各个字段，不需要全填，最少填：name / description / prompt / examples",
    "  4. 放到 config/skills/ 目录，重启 Koto 即可生效"
  ],

  "id": "my_custom_skill",
  "_id_comment": "唯一ID，小写字母+下划线，不能和其他 skill 重复",

  "name": "我的自定义技能",
  "icon": "✨",
  "_icon_comment": "直接输入一个 emoji，会显示在 UI 上",

  "category": "domain",
  "_category_comment": "三选一：behavior（改变 AI 行为）| style（改变风格）| domain（专业领域技能）",

  "description": "简短描述这个 skill 是干什么的（20 字以内，显示在 Skill 列表里）",

  "intent_description": "描述什么情况下应该自动激活这个 skill（30 字以内，AI 匹配意图用）",
  "_intent_description_comment": "例：'用户需要分析、总结、改写某类文本时'",

  "prompt": "\n\n## ✨ 领域要求：[技能名称]\n\n你是一名 [角色描述]。\n\n当用户提交 [输入类型] 时，你必须：\n1. [第一步要做什么]\n2. [第二步要做什么]\n3. [第三步要做什么]\n\n### 输出格式\n[精确描述格式，比如：Markdown 表格 / JSON / 带编号的列表]\n\n### 不允许\n- [禁止做的事情1]\n- [禁止做的事情2]",
  "_prompt_comment": [
    "prompt 是最核心的字段，决定 AI 怎么工作。",
    "Anthropic 建议的结构：角色 → 任务 → 格式 → 约束",
    "越具体越好，用 '必须' '不允许' 代替模糊词语。",
    "写完 prompt 后，写几个 examples（下面的字段），效果会大幅提升。"
  ],

  "examples": [
    {
      "_comment": "examples 是 Anthropic 强调的最重要元素。写 2-3 个就够了。",
      "input": "用户输入的例子（真实情境，越具体越好）",
      "output": "期望的模型输出（格式要和 prompt 里描述的一致）",
      "note": "这个例子展示什么特点（可选，帮助自己记忆）"
    },
    {
      "input": "另一个输入例子（最好覆盖不同情况）",
      "output": "对应的期望输出",
      "note": "边界情况 / 特殊格式的例子"
    }
  ],
  "_examples_comment": [
    "为什么 examples 这么重要？",
    "  · 模型能从例子里学到 prompt 文字没法准确传达的格式细节",
    "  · 小白写的 prompt 往往模糊，但例子是明确的",
    "  · 有 2-3 个例子的 skill，比没有例子的 skill 一致性高 3-5 倍",
    "",
    "写好例子的技巧：",
    "  · 用真实场景，不要写 '测试输入'",
    "  · input 和 output 格式要和你在 prompt 里描述的完全一致",
    "  · 至少一个 '典型例子' + 一个 '边界/特殊例子'"
  ],

  "output_spec": {
    "format": "markdown",
    "_format_comment": "markdown | plain | json | table | code | any",
    "must_contain": [],
    "_must_contain_comment": "输出中必须包含的字符串列表，用于自动质量检查。例如 ['建议', '##']",
    "must_not_contain": [],
    "min_chars": null,
    "max_chars": null,
    "required_json_keys": [],
    "description": "在这里描述期望的输出格式（比 output_spec.format 更详细的文字说明）"
  },

  "task_types": ["CHAT"],
  "_task_types_comment": [
    "决定这个 skill 在哪些任务类型下生效。常用：",
    "  CHAT        通用对话（绝大多数情况）",
    "  DOC_ANNOTATE 文档批注任务",
    "  CODER       写代码任务",
    "  FILE_GEN    生成文件任务",
    "  RESEARCH    研究/查询任务",
    "留空 [] 或省略 = 所有任务类型都生效"
  ],

  "bound_tools": [],
  "_bound_tools_comment": "这个 skill 推荐使用的工具名列表。填了之后 AI 会优先调用这些工具。常见工具：read_file, write_file, web_search, list_directory, annotate_document",

  "enabled": false,
  "_enabled_comment": "false = 默认关闭，用户在 UI 里手动开启 | true = 默认开启",

  "tags": ["自定义", "示例"],
  "_tags_comment": "搜索标签，中英文均可，方便在 Skill 市场里被找到",

  "priority": 50,
  "_priority_comment": "0-100，数字越大越先注入到 prompt。默认 50，重要 skill 可设 70-80",

  "version": "1.0.0",
  "author": "your_name",
  "created_at": "2026-03-14T00:00:00Z",

  "_advanced_comment": [
    "====================================================================",
    "  进阶字段（小白可以忽略，熟练后再添加）",
    "====================================================================",
    "",
    "plan_template  : 让这个 skill 变成「确定性流程」，不依赖 AI 自己想步骤",
    "                 适合多步骤、固定流程的任务（批注 → 分析 → 汇总）",
    "",
    "executor_tools : AI 执行这个 skill 时，只允许调用哪些工具",
    "                 减少 AI 误用其他工具，让结果更可预期",
    "",
    "entry_point    : 绑定一个 Python 函数实现，让 skill 直接调用真实代码",
    "                 格式：'module.path:function_name'"
  ],

  "plan_template": [],
  "executor_tools": [],
  "entry_point": null,

  "_v3_dynamic_comment": [
    "====================================================================",
    "  v3 动态编排字段（高级）——强化模型工具调用与多模型协作能力",
    "====================================================================",
    "",
    "下面 4 个字段是 Koto Skill v3 新增的「动态编排」能力。",
    "它们在 plan_template（固定步骤）的基础上，进一步教会模型：",
    "  · 根据输入动态选择工具（tool_orchestration）",
    "  · 按照 ReAct 推理循环执行任务（react_protocol）",
    "  · 在多模型/多 Agent 管线中合理分工（multi_model_hint）",
    "  · 为下游 skill（chains_to 列表中的技能）准备格式化输出（handoff_context）",
    "",
    "✅ 全部字段默认为空字符串，留空不影响现有 skill 行为。",
    "✅ 只在你需要精细控制工具调用或 multi-agent 协作时才填写。"
  ],

  "tool_orchestration": "",
  "_tool_orchestration_comment": [
    "【用途】告诉模型「在什么情况下调用哪个工具」，用条件树格式书写。",
    "【格式】用 • 和缩进表示条件分支，用 → 表示动作：",
    "",
    "示例：",
    "• 用户提供了文件路径 → read_file_snippet(path, max_chars=5000)",
    "  · 返回内容被截断（末尾有 '...'）→ summarize_file(path, focus='问题相关代码')",
    "  · 返回乱码/空内容 → 告知用户文件可能损坏，建议手动检查",
    "• 用户只提供了文字描述（无路径）→ 直接分析，不调用文件工具",
    "  · 需要验证假设 → 调用 execute_python 运行最小验证代码",
    "",
    "【技巧】",
    "  - 越具体越好：与其说「需要时调用工具」，不如写「满足 X 条件 → 调用 Y」",
    "  - 写上「工具失败时怎么办」能显著提升鲁棒性",
    "  - executor_tools 字段控制「哪些工具可用」，tool_orchestration 控制「怎么用」"
  ],

  "react_protocol": "",
  "_react_protocol_comment": [
    "【用途】控制模型是否使用 ReAct（Reason + Act）推理循环。",
    "【取值】三选一：",
    "  ''      → 不注入任何 ReAct 提示（默认，适合简单对话 skill）",
    "  'light' → 注入一行提醒：调用工具前先说明理由，工具结束后总结",
    "            适合需要 1-3 个工具调用的中等复杂 skill",
    "  'full'  → 注入完整 4 步循环：思考 → 行动 → 观察 → 重复",
    "            适合需要多轮工具调用、决策分支多的复杂 skill",
    "",
    "【建议】",
    "  - 简单的 CHAT skill → 留空 ''，避免无谓 token 开销",
    "  - 有 tool_orchestration 的 skill → 至少用 'light'",
    "  - plan_template 超过 4 步 + 多种工具 → 用 'full'"
  ],

  "multi_model_hint": "",
  "_multi_model_hint_comment": [
    "【用途】为 Koto 的多模型/多 Agent 管线提供协作建议。",
    "【格式】自由文本，建议用 • 列出：模型偏好 + Agent 分工 + 节点间数据传递。",
    "",
    "示例：",
    "• 复杂分析任务（多层逻辑推理）→ 优先使用高推理能力模型（Gemini 2.5 Pro）",
    "• 简单格式化/代码生成 → 可使用更快的本地模型降低延迟",
    "• MultiAgentOrchestrator 推荐分工：",
    "  - 节点 1（数据收集+工具调用）：速度优先的模型",
    "  - 节点 2（深度分析+洞察生成）：推理能力优先的模型",
    "  - 节点 1 → 节点 2 传递的关键字段：raw_data, context_summary",
    "",
    "【注意】这是建议性字段，最终路由决策由 MultiAgentOrchestrator 执行。",
    "       填写后，编排器会将其作为参考权重，但不强制覆盖用户设置。"
  ],

  "handoff_context": "",
  "_handoff_context_comment": [
    "【用途】当此 skill 通过 chains_to 链接到下游 skill 时，描述应该输出哪些字段",
    "       来让下游 skill 顺利接手任务（减少「上游输出 ≠ 下游期望输入」的断层）。",
    "【格式】自由文本，建议按下游 skill 分段描述：",
    "",
    "示例（debug_python → write_unit_tests + git_commit_helper）：",
    "为 write_unit_tests 准备的数据：",
    "- 修复后函数的完整签名（名称/参数/返回类型注解）",
    "- 已识别的边界条件（None/空列表/负数/编码/线程锁等）",
    "- 建议的测试场景：正常路径 + 2 个边界场景 + 1 个错误路径",
    "",
    "为 git_commit_helper 准备：",
    "- 一句话修复摘要，格式：fix: [模块名] [修复内容]",
    "",
    "【技巧】",
    "  - 只需描述下游 skill 真正需要的字段，不要泛泛而谈",
    "  - 格式越精确，下游 skill 的输入质量越高"
  ]
}
