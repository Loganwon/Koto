# ===== Koto - Python Dependencies =====
# pip install -r requirements.txt

# ── Core (必须) ──
flask>=3.0.0
Flask-WTF>=1.2.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
flask-sock>=0.7.0
websocket-client>=1.8.0
python-dotenv>=1.0.0
google-genai>=1.62.0
openai>=2.41.0,<3
requests>=2.31.0
httpx>=0.27.0
psutil>=5.9.0
markdown>=3.5.0
gunicorn>=22.0.0           # 生产环境 WSGI 服务器
python-json-logger>=4.1.0  # Structured JSON logging (KOTO_LOG_FORMAT=json)
sentry-sdk[flask]>=2.0.0   # Error tracking (set SENTRY_DSN to enable)
prometheus-flask-exporter>=0.23.2  # Metrics endpoint at /metrics
flasgger>=0.9.7             # Swagger UI at /apidocs/ (set SWAGGER_ENABLED=true)

# ── Document Processing ──
python-docx>=1.0.0
lxml>=5.0.0
pypdf>=4.0.0                   # PDF 文本提取（轻量，纯Python）
pdfplumber>=0.10.0
python-pptx>=0.6.23
openpyxl>=3.1.5
mammoth>=1.6.0                 # .docx → 语义 HTML（DocxNormalizer）
html2docx>=0.0.6               # HTML → .docx 导出（文件助手工作区）
reportlab>=4.0.0               # PDF 文档生成（doc_gen_service）
beautifulsoup4>=4.12.0
jieba>=0.42.1              # 中文分词
docx2txt>=0.8              # .doc / .docx 文本提取（无需 Word COM）
striprtf>=0.0.26           # RTF 纯文本提取
EbookLib>=0.18             # .epub 文本提取

# ── Data ──
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.8.0

# ── Image ──
Pillow>=10.0.0

# ── LoRA 蒸馏训练（可选，不安装时骨架模式运行）──
# 普通用户 / 发行版无需安装，未装时自动降级为"骨架模式"
# 如需在本机启用真实 GPU 训练，单独执行：
#   pip install -r config/requirements_training.txt

# ── Scheduling ──
schedule>=1.2.2
watchdog>=4.0.0            # 文件夹监控 (Watch Mode)

# ── LangChain / LangGraph ──
langchain-core>=0.3.0
langchain-google-genai>=2.0.0   # ChatGoogleGenerativeAI wrapper
langgraph>=0.2.0                 # StateGraph 状态机工作流
langgraph-checkpoint>=1.0.0      # 检查点持久化基类
langgraph-checkpoint-sqlite>=1.0.0  # SqliteSaver 持久化检查点（跨重启续跑）
langchain-community>=0.3.0       # 文档加载器 / Vector store 工具

# ── Security ──
cryptography>=42.0.0
PyJWT>=2.8.0               # JWT token auth for SaaS

# ── Desktop (Windows only, optional for server mode) ──
pywebview>=5.0.0
pystray>=0.19.0
pyperclip>=1.8.2
# pywin32>=306
# win10toast>=0.9
# wmi>=1.5.1

# ── Upload STT (optional) ──
# Koto records audio in the browser and uploads it to local Whisper/Gemini.
# Retired desktop microphone stacks must stay out of this file.
# ── 本地离线上传语音识别（可选，安装后自动替代 Gemini STT，无需 API Key）──
# faster-whisper>=1.0.0    # 推荐：pip install faster-whisper
# openai-whisper>=20231117 # 备选：pip install openai-whisper

# ── OCR (optional) ──
# pytesseract>=0.3.10

# ── File Operations ──
send2trash>=1.8.3
