[pytest]
pythonpath=.
cache_dir=.pytest_cache_local
markers =
    e2e: browser-driven end-to-end regression coverage
    integration: integration coverage that crosses application boundaries
    live_api: tests that require an available external provider or API
    unit: isolated unit regression coverage
